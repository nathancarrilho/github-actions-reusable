name: Reusable - Criar Issue no Repositório 🚨

on:
  workflow_call:
    inputs:
      issue_title:
        description: 'Título completo do Issue a ser criado.'
        required: true
        type: string
      issue_body:
        description: 'Corpo (descrição detalhada) do Issue a ser criado.'
        required: true
        type: string

jobs:
  create_github_issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: nashmaniac/create-issue-action@v1.2
        name: Create Issue Action
        with:
          title: ${{ inputs.issue_title }}
          token: ${{secrets.GITHUB_TOKEN}}
          assignees: ${{github.actor}}
          body: ${{ inputs.issue_body }}